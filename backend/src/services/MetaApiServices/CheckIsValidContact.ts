const CheckIsValidContact = async (number: string): Promise<void> => {
  // TODO: Meta doesn't have a direct "is valid" endpoint like wwebjs.
  // We assume true or validate via a different method.
  return;
};

export default CheckIsValidContact;
