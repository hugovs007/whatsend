const ImportContactsService = async (whatsappId: number): Promise<void> => {
  // TODO: Implement syncing logic if possible via Meta API, or ignore
  return;
};

export default ImportContactsService;
