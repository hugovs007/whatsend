const CheckNumber = async (number: string): Promise<string> => {
  // TODO: Implement Meta API logic if applicable, or just return the sanitized number
  return number;
};

export default CheckNumber;
